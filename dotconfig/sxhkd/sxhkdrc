#
# bspwm hotkeys
#

super + alt + Escape
	bspc quit

super + w
	bspc node -c

# super + t
# 	bspc desktop -l next

super + b
	bspc desktop -B

super + {t,shift + t,s,f}
  bspc node -t {"~tiled","~pseudo_tiled","~floating","~fullscreen"}

super + {grave,Tab}
	bspc {node,desktop} -f last

super + apostrophe
	bspc node -s last

super + {o,i}
	bspc control --record-history off; \
	bspc node {older,newer} -f; \
	bspc control --record-history on

super + y
	bspc node -w last.manual

super + m
	bspc node -s biggest

#super + {_,shift + }{h,j,k,l}
#	bspc node -{f,s} {west,south,north,east}

super + {_,shift + }c
	bspc node -f {next,prev}

super + {comma,period}
	bspc desktop -C {backward,forward}

super + bracket{west,east}
	bspc desktop -f {prev,next}

super + ctrl + {h,j,k,l}
	bspc node -p {west,south,north,east}

super + ctrl + {_,shift + }space
	bspc {node -p cancel,desktop -c}

super + alt + {h,j,k,l}
	bspc node -e {west -10,south +10,north -10,east +10}

super + alt + shift + {h,j,k,l}
	bspc node -e {east -10,north +10,south -10,west +10}

super + ctrl + {1-9}
	bspc node -r 0.{1-9}

super + {_,shift + }{1-9,0}
	bspc {desktop -f,node -d} ^{1-9,10}

~button1
	bspc node pointed -f

#Mouse stuff
# start move/resize
# super + button{1-3}
    # bspc pointer -g {move,resize_side,resize_corner}

# super + button{1-3}
    # bspc pointer -g {move,resize_side,resize_corner}

# super + !button{1-3}
    # bspc pointer -t %i %i

# end move/resize
#super + @button{1-3}
#    bspc pointer -u

# super + !button{1-3}
# 	bspc pointer --track %i %i

# super + @button{1-3}
	# bspc pointer -u

#
# wm independent hotkeys
#

super + Return
  terminator

super + space
	dmenu_run

# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd


##########################
#System
##########################

# Raises volume
XF86AudioRaiseVolume
    notify_volume + 5
    #amixer set Master 2%+

# Lowers volume
XF86AudioLowerVolume
    notify_volume - 5

# Mutes
XF86AudioMute
    amixer set Master toggle

# Beastness goes up
XF86MonBrightnessUp
    brightnessScript +

# Beastness goes south
XF86MonBrightnessDown
    brightnessScript -

# Screen lock
XF86ScreenSaver
    blur

# Turns off laptop
XF86Launch1
    systemctl poweroff

# Reloads bspwm..don't know if it works
ctrl + super + r
    bspc reload

##############
# Custom
##############

ctrl + Print
  /home/$USER/bin/screenshot/upscreenshot.sh -s

Print
  /home/$USER/bin/screenshot/upscreenshot.sh

super + l
  /usr/bin/xscreensaver-command -lock

super + ctrl + shift + space
  /home/$USER/bin/monitors/toggle.sh
